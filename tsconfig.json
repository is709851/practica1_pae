{
    "include": ["src/**/*.ts"],
    "exclude": ["src/**/*.spec.ts"],
    "compilerOptions": {
        "outDir": "dist/assets/scripts", //Lugar donde vamos a poner los archivos transformados
        "target":"ES5" //Version por defecto de EScript
    }
}